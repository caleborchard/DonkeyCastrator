# DonkeyCastrator

A MelonLoader mod for Baby Steps that automatically censors the donkey genitalia to a fuller extent by scaling them to zero.
There are no in-game toggles, so to disable the mod just remove it from the Mods folder.